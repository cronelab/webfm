import React from 'react'
import Head from 'next/head'

function HomePage({ subjects }) {
  return (
    <div>
      <Head>
        <title>WebFM</title>
        <meta charSet="utf-8" />
      </Head>
    </div>
  )
}

export default HomePage
